name: 2. Deploy State

on:
  workflow_dispatch:
    # Optionale Eingabe, falls Sie die Run-ID des ersten Workflows benötigen.

        
# Konkurrenz-Kontrolle: Stellt sicher, dass nur ein Workflow-Lauf gleichzeitig aktiv ist.
concurrency: 
  group: workflow_state_update
  cancel-in-progress: true 

env:
  ENC_KEY: ${{ secrets.STATE_ENCRYPTION_KEY }} # Encryption Key als Environment Variable
  
jobs:
  # Job 1: Alten State holen, entschlüsseln und Vorprüfung durchführen
  prepare_assessment:
    runs-on: self-hosted
    permissions:
      contents: write 
    steps:
      - name: Code auschecken
        uses: actions/checkout@v4
        
      - name: Alten State holen & entschlüsseln
        run: |
          git fetch origin state-storage:state-storage || true
          git checkout state-storage -- state_file.enc 2>/dev/null || true
          git checkout main 

          if [ -f state_file.enc ]; then
            echo "Alten State entschlüsselt."
            openssl aes-256-cbc -d -a -in state_file.enc -out state_file.json -pass env:ENC_KEY
            rm state_file.enc
          fi


      - name: Kompilierung und Update der State-Datei
        # Ihr Hauptskript aktualisiert nun state_file.json (den NEUEN Zustand)
        run: echo "next" >> state_file.json
        
      - name: Neuen State verschlüsseln (Nur wenn geändert) 🔒
        run: |
          mkdir -p output
          openssl aes-256-cbc -a -salt -in state_file.json -out output/state_file.enc -pass env:ENC_KEY
          
      - name: Verschlüsselten State auf separaten Branch pushen 💾
        uses: s0/git-publish-subdir-action@v2
        with:
          # VORHER: Der REPO-Parameter fehlte, obwohl er implizit "self" sein sollte
          # NEU: Definiert explizit das Ziel-Repository
          repo: ${{ github.repository }} 
          
          # NEU: Alternativ kann 'repo: self' verwendet werden, erfordert aber einen GITHUB_TOKEN in env
          # repo: self 

          # Der Ordner, der die finale State-Datei enthält
          src: "output" 
          # Der Branch, auf dem der State gespeichert wird
          branch: "state-storage" 
          # Die verschlüsselte Datei
          file_pattern: state_file.enc
          # Commit-Nachricht
          commit_message: "Auto-update encrypted state [skip ci]"
          
        env:
          # Der GITHUB_TOKEN muss in der env-Sektion verfügbar sein, um Push-Rechte zu haben.
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
