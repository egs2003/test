name: Vorprüfung

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths-ignore:
      - '.github/**'
      - 'README.md'
      
# Konkurrenz-Kontrolle: Stellt sicher, dass nur ein Workflow-Lauf gleichzeitig aktiv ist.
concurrency: 
  group: workflow_state_update
  cancel-in-progress: true 

env:
  ENC_KEY: ${{ secrets.STATE_ENCRYPTION_KEY }} # Encryption Key als Environment Variable

jobs:
  prepare_assessment:
    runs-on: self-hosted
    outputs:
      state_available: ${{ steps.fetch_state.outputs.state_available }}
      assessment: ${{ steps.assessment.outputs.LastResult }}
    steps:
      - name: Code auschecken
        uses: actions/checkout@v4
        
      - name: Alten State holen & entschlüsseln
        id: fetch_state
        run: |
          git fetch origin state-storage:state-storage || true
          git checkout state-storage -- state_file.enc 2>/dev/null || true

          if [ -f state_file.enc ]; then
            echo "Alten State entschlüsselt."
            openssl aes-256-cbc -d -a -in state_file.enc -out state_file.json -pass env:ENC_KEY -pbkdf2
            rm state_file.enc
            echo "state_available=true" >> $GITHUB_OUTPUT
          fi

      - name: Vorprüfung (Einschätzung) durchführen
        id: assessment
        run: |
          test=$(echo "test123")
          echo "LastResult=$test" >> $GITHUB_OUTPUT

  manual_approval:
    needs: prepare_assessment
    runs-on: self-hosted
    environment: 
      name: approve
    steps:
      - name: dummy
        run: echo "Hallo Welt"
    
  compile_and_deploy:
    needs: manual_approval
    runs-on: self-hosted
    permissions:
      contents: write
    steps:
      - name: Code auschecken
        uses: actions/checkout@v4
        
      - name: Alten State holen & entschlüsseln
        id: fetch_state
        run: |
          git fetch origin state-storage:state-storage || true
          git checkout state-storage -- state_file.enc 2>/dev/null || true

          if [ -f state_file.enc ]; then
            echo "Alten State entschlüsselt."
            openssl aes-256-cbc -d -a -in state_file.enc -out state_file.json -pass env:ENC_KEY -pbkdf2
            rm state_file.enc
            echo "state_available=true" >> $GITHUB_OUTPUT
          fi

      - name: Kompilierung und Update der State-Datei
        # Ihr Hauptskript aktualisiert nun state_file.json (den NEUEN Zustand)
        run: |
          date >> state_file.json
          cat state_file.json

      - name: Neuen State verschlüsseln (Nur wenn geändert) 🔒
        run: |
          mkdir -p output
          openssl aes-256-cbc -a -salt -in state_file.json -out output/state_file.enc -pass env:ENC_KEY -pbkdf2
          
      - name: Verschlüsselten State auf separaten Branch pushen (Manueller Git-Block) 💾
        run: |
          git config user.name "State Update Bot"
          git config user.email "actions@github.com"
          
          git checkout state-storage || git checkout --orphan state-storage

          cp output/state_file.enc .
          
          git add state_file.enc
          git commit -m "Auto-update encrypted state"
          
          git push origin state-storage
        
  
    

    
